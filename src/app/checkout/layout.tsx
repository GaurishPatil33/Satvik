export default function CheckoutLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>
            {/* No Header / Footer here */}
            {children}
        </>
    );
}